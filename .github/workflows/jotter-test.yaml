name: "Jotter Test"

on: [pull_request]

jobs:
  test:
    name: '${{ matrix.test }}'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test:
          - short-internet

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 1

    - name: apt-get install packages
      run: sudo apt-get update -qq &&
           sudo apt-get install --no-install-recommends -y
               bison
               build-essential
               ccache
               check
               clang
               flex
               git
               gperf
               libcurl4-openssl-dev
               libgtk-3-dev
               libhtml-parser-perl
               libjpeg-dev
               libpng-dev
               librsvg2-dev
               libutf8proc-dev
               llvm
               pkg-config
               wbritish # Needed for `/usr/share/dict/words`, used by test

    - name: ccache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ github.job }}
        max-size: 128M

    - name: Build and install project libs
      env:
        REPO_BASE_URI: https://github.com/gabrieldiego
      run: |
          export TARGET_WORKSPACE="$(pwd)/projects"
          source docs/env.sh
          slate-clone -d -s -b ${GITHUB_REF_NAME}
          slate-make-tools install
          slate-make-libs install

    - name: Disable -Werror
      # We can remove this step if we ever move to GitHub properly.
      run: |
          find . -type f -name Makefile | xargs sed -i 's/-Werror//'

    - name: Build Slate Jotter
      run: |
          export TARGET_WORKSPACE="$(pwd)/projects"
          source docs/env.sh
          make -j"$(nproc)" TARGET=jotter

    - name: Run Jotter Test
      run: |
          LC_ALL=C.UTF-8 test/jotter-see-jotter-do -v -d ${{ matrix.test }}
